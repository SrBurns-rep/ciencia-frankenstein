// function encrypt(value: string, key: string) {
// 	return value + " (encrypted)";
// }
// function decrypt(value: string, key: string) {
// 	return value.replace(" (encrypted)", "");
// }

// function createUser(name: string) {
// 	let key = {
//         plain: ""
//         // public:
//     }

//     return {
// 		name,
//         key,
// 		sendMessage(user: any, message: string) {
// 			user.receiveMessage(message);
// 		},
// 		receiveMessage(message: string) {},
// 	};
// }

// let cien = createUser("Cien");
// let marcus = createUser("Marcus");

// // Cien me dá chave pública
// // cien.
// // cien.sendMessage(marcus, )
// // Marcus gera chave secreta
// // Marcus manda para Cien a chave secreta de Marcus criptografada com a chave pública de Cien
// // Cien descriptografa chave secreta de Marcus com a Chave secreta de Cien
